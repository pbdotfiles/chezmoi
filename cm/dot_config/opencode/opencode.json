{
  "$schema": "https://opencode.ai/config.json",

  // One-time manual step on each machine: npx @playwright/mcp install-browser chrome-for-testing
  "mcp": {
    "playwright": {
      "type": "local",
      "command": [
        "npx",
        "@playwright/mcp@latest",
        "--caps=vision,devtools",
        "--browser=chromium"
      ],
      "enabled": true
    }
  }
}
